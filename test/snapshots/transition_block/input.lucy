use './stuff.js' { check }

guard canGo = check
guard sureCanGo = check
guard AreWeReallySure = check

initial state start {
  go => {
    canGo =>
      sureCanGo =>
      AreWeReallySure =>
      end
  }
}

final state end {

}